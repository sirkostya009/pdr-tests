declare global {
	namespace App {
		// interface Error {}
		// interface Locals {}
		// interface PageData {}
		interface PageState {
			finished?: boolean;
		}
		// interface Platform {}
	}
}

export {};
